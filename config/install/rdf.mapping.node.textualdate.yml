langcode: en
status: true
dependencies:
  config:
    - node.type.textual_date
  enforced:
    module:
      - dragomans
  module:
    - node
id: node.textual_date
targetEntityType: node
bundle: textual_date
types:
  - 'schema:Thing'
fieldMappings:
  title:
    properties:
      - 'schema:date'
  field_date_type:
    properties:
      - 'schema:datetype'
