langcode: en
status: true
dependencies:
  config:
    - taxonomy.vocabulary.event_types
  enforced:
    module:
      - dragomans
  module:
    - taxonomy
id: taxonomy_term.event_types
targetEntityType: taxonomy_term
bundle: event_types
types:
  - 'schema:Thing'
fieldMappings:
  name:
    properties:
      - 'schema:name'
  description:
    properties:
      - 'schema:description'
