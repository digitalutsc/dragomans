langcode: en
status: true
dependencies:
  config:
    - node.type.topic
  enforced:
    module:
      - dragomans
  module:
    - node
id: node.topic
targetEntityType: node
bundle: topic
types:
  - 'dbpedia-ow:TopicalConcept'
fieldMappings:
  title:
    properties:
      - 'dbpedia-owl:topic'
  field_notes:
    properties:
      - 'dbpedia-owl:notes'
