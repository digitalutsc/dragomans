langcode: en
status: true
dependencies:
  config:
    - node.type.event_type
  enforced:
    module:
      - dragomans
  module:
    - node
id: node.event_type
targetEntityType: node
bundle: event_type
types:
  - 'schema:Thing'
fieldMappings:
  title:
    properties:
      - 'schema:name'
  field_description:
    properties:
      - 'schema:description'
  field_notes:
    properties:
      - 'dbpedia-owl:notes'
