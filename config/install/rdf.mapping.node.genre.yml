langcode: en
status: true
dependencies:
  config:
    - node.type.genre
  enforced:
    module:
      - dragomans
  module:
    - node
id: node.genre
targetEntityType: node
bundle: genre
types:
  - 'dbpedia-owl:Genre'
fieldMappings:
  title:
    properties:
      - 'schema:genre'
  field_notes:
    properties:
      - 'dbpedia-owl:notes'
